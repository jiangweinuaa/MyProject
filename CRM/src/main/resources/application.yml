server:
  port: 8081
  servlet:
    context-path: /api

spring:
  application:
    name: crm-card-service
  datasource:
    url: jdbc:mysql://localhost:3306/openclaw_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: openclaw_user
    password: j7gLZHHYGcAeOTcy
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        physical_naming_strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

logging:
  level:
    root: INFO
    com.crm: DEBUG
    org.springframework.web: INFO
